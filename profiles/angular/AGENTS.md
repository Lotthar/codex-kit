# Angular profile

Follow the workspace's Angular CLI or Nx conventions. Reuse the existing standalone or NgModule style, run the narrowest configured lint, test, and build scripts, and prefer affected-project commands in a monorepo.
